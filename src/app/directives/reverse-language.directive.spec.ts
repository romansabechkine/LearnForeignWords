import { ReverseLanguageDirective } from './reverse-language.directive';

describe('ReverseLanguageDirective', () => {
  it('should create an instance', () => {
    const directive = new ReverseLanguageDirective();
    expect(directive).toBeTruthy();
  });
});

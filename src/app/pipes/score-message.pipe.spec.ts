import { ScoreMessagePipe } from './score-message.pipe';

describe('ScoreMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new ScoreMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
